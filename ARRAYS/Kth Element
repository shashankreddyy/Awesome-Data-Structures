#include <iostream>
#include <bits/stdc++.h> 
using namespace std;

int main() {
	int n,d;
	cin>>n>>d;
	int arr[n];
	for(int i=0;i<n;i++)
	{
	    cin>>arr[i];
	}
	int s = sizeof(arr)/sizeof(arr[0]);
	
	sort(arr, arr+s);
	
	cout<<arr[d-1];
	
	return 0;
}
